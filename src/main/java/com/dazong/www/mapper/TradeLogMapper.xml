<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dazong.www.mapper.TradeLogMapper">
    <!-- 这里namespace必须是UserMapper接口的路径” -->
    <insert id="insertTradeLog" parameterType="com.dazong.www.pojo.TradeLog">

    </insert>

    <!-- 这里的id必须和UserMapper接口中的接口方法名相同 -->
    <select id="getTradeLog" resultType="com.dazong.www.pojo.TradeLog" parameterType="java.lang.Long">

    </select>
</mapper>